{
  "name": "sequence-predictor-server",
  "version": "1.0.0",
  "description": "Express server for serving sequence predictor results",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "node test/announcements.test.js && node test/jobs.test.js && node test/council.test.js && python3 -m src.TuningScore && python3 -m src.TuningGate"
  },
  "nodemonConfig": {
    "ignore": [
      "data/*",
      "hyperOptCache/*",
      "trainingData/*",
      "config/*"
    ]
  },
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.1.9"
  }
}
